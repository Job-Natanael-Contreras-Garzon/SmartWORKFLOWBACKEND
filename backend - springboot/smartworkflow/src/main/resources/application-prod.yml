server:
  port: 8080
 
spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
 
  flyway:
    clean-disabled: true
 
  mail:
    properties:
      mail:
        smtp:
          ssl:
            trust: smtp.gmail.com
 
logging:
  level:
    root: WARN
    com.example.smartworkflow: INFO